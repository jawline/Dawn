The specification for the filesystem API

The following structures are defined by the API

directory entry
512byte character array containing the filename
unsigned int containing the inode used by some mount's

the handle FILE is also defined as a unsigned long.

The following functions are defined
