#ifndef _SCREEN_DEF_H_
#define _SCREEN_DEF_H_
#include "int_types.h"

static void text_mode_clearscreen();
static void text_mode_putc(char Character);
static void text_mode_write(char * Array);

#endif
